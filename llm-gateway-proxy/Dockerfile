# OpenAI 互換プロキシ（モデルエイリアス → rust-inference / llama-server）
FROM rust:1.86-bookworm AS build
WORKDIR /app
COPY Cargo.toml Cargo.lock ./
COPY src ./src
RUN cargo build --release

FROM debian:bookworm-slim
RUN apt-get update \
    && apt-get install -y --no-install-recommends ca-certificates curl \
    && rm -rf /var/lib/apt/lists/*
COPY --from=build /app/target/release/llm-gateway-proxy /usr/local/bin/
EXPOSE 4100
ENV BIND_ADDR=0.0.0.0:4100
CMD ["llm-gateway-proxy"]
