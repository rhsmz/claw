# Skill: team_notify (deprecated alias)

**Use instead**: `team_notify_discord.md` or `team_notify_slack.md` per crew configuration in `zeroclaw/config.toml`.

**MCP / tool boundary**: Same as the concrete notifier skill you select.

## Purpose

Legacy id retained for documentation only. All crew `skills` arrays should reference `team_notify_discord` or `team_notify_slack`.

## Procedure

1. Open the Discord or Slack skill card referenced from config.
2. Follow that card’s procedure.
